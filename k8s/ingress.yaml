apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: uku_gitops
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: uku_gitops
                port: 
                  number: 80
